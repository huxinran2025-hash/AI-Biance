# BIANCE 系统启动服务器问题排查总结

## 🚨 问题概述

BIANCE 量化交易系统在启动开发服务器（Vite）后，浏览器访问 `http://localhost:3005/` 出现白屏，控制台报大量错误。

### 快速诊断（TL;DR）

| 项目 | 详情 |
|------|------|
| **问题类型** | 架构设计错误 - 前后端代码严重耦合 |
| **核心错误** | `Module "node:fs/promises" has been externalized for browser compatibility` |
| **根本原因** | 前端 React 代码直接导入了 15+ 个使用 Node.js 模块的后端文件 |
| **影响范围** | 整个应用无法启动，完全白屏 |
| **推荐方案** | 前后端完全分离（架构重构） |
| **备选方案** | Electron 桌面应用 / Mock + 逐步迁移 |
| **修复难度** | ⭐⭐⭐⭐⭐ (需要架构级重构) |
| **预计工作量** | 3-5 天 |

**环境信息：**
- 操作系统: Windows 10 (10.0.26100)
- Node.js: (使用 npm)
- 包管理器: npm
- 前端框架: React 19.2.0
- 构建工具: Vite 6.2.0
- TypeScript: 5.8.2

## 核心错误

### 1. Node.js 模块浏览器兼容性问题

#### 错误信息：
```
Module "node:fs/promises" has been externalized for browser compatibility. 
Cannot access "node:fs/promises.readFile" in client code.
at binance-whitelist.ts:1:26
```

#### 错误信息 2：
```
Module "events" has been externalized for browser compatibility. 
Cannot access "events.EventEmitter" in client code.
at exchangeRiskWatcher.ts:2:30
```

### 2. WebSocket 连接失败

```
[vite] server connection lost. Polling for restart...
WebSocket connection to 'ws://localhost:3005/' failed
```

多次尝试连接 3005、3006 端口均失败。

### 3. Cloudflare Workers 连接失败

```
Failed to load resource: net::ERR_INTERNET_DISCONNECTED
- @cloudflare/workers-3000
- @cloudflare/workers-3006
```

## 已执行的修复措施

### 修复 1: 移除 riskManagerDecision.ts 中的模板字符串编码问题

**问题：** 第37行、83-103行中文字符串导致 esbuild 解析错误
- 症状: `Expected "}" but found "Retry"`, `Expected ":" but found "€"`
- 位置: `backend/strategist/riskManagerDecision.ts`

**解决方案：**
1. 创建新文件 `backend/strategist/riskPromptSections.ts`
2. 将所有中文提示文本改为纯 ASCII 英文
3. 使用辅助函数 `buildRiskPromptSections()` 构建提示内容
4. 移除文件的 UTF-8 BOM 标记

**修改文件：**
- `backend/strategist/riskManagerDecision.ts`
- 新增 `backend/strategist/riskPromptSections.ts`

### 修复 2: 变量名冲突

**问题：** 函数参数 `reason` 与局部变量重复声明
- 错误: `The symbol "reason" has already been declared`
- 位置: `backend/strategist/riskManagerDecision.ts:213`

**解决方案：**
```typescript
// 将局部变量重命名
const parsedReason = typeof parsed.reason === 'string' ? parsed.reason.trim() : 'No reason provided';

// 更新返回对象
return {
    masterRiskScore,
    reason: parsedReason,
};
```

### 修复 3: 缺失的 API 导出函数

**问题：** `resetPaperAccount`, `resetShadowAccount`, `stopLiveTrading` 未导出
- 错误: `does not provide an export named 'resetPaperAccount'`
- 位置: `backend/api.ts`

**解决方案：**
在 `backend/api.ts` 末尾添加：
```typescript
export async function resetPaperAccount(): Promise<void> {
    auditService.logInfo('[API] Resetting paper trading account');
    state = getInitialState(liveConfig);
    shadowLedger.length = 0;
}

export async function resetShadowAccount(): Promise<void> {
    auditService.logInfo('[API] Clearing shadow ledger');
    shadowLedger.length = 0;
}

export async function stopLiveTrading(): Promise<void> {
    auditService.logInfo('[API] Stopping live trading');
    if (state && state.systemStatus) {
        state.systemStatus.tradeExecutionEnabled = false;
    }
}
```

### 修复 4: Node.js events 模块浏览器兼容性

**问题：** `exchangeRiskWatcher.ts` 使用了 Node.js 的 `events` 模块
- 错误: `Module "events" has been externalized for browser compatibility`

**解决方案：**
1. 创建浏览器兼容的 EventEmitter: `backend/utils/EventEmitter.ts`
```typescript
export class EventEmitter {
    private events: Map<string, Function[]> = new Map();
    
    on(event: string, listener: Function): this { ... }
    emit(event: string, ...args: any[]): boolean { ... }
    off(event: string, listener: Function): this { ... }
    removeAllListeners(event?: string): this { ... }
}
```

2. 更新导入：
```typescript
// 从
import { EventEmitter } from 'events';
// 改为
import { EventEmitter } from './utils/EventEmitter';
```

### 修复 5: Node.js fs/promises 模块浏览器兼容性

**问题：** `config/binance-whitelist.ts` 使用 `node:fs/promises`
- 错误: `Module "node:fs/promises" has been externalized for browser compatibility`

**解决方案：**
```typescript
// 从
import { readFile } from 'node:fs/promises';
export async function loadWhitelist(path = resolve(__dirname, 'binance-whitelist.json')): Promise<Map<string, RiskTierKey>> {
    const raw = await readFile(path, 'utf-8');
    const parsed = JSON.parse(raw) as WhitelistConfig;
    // ...
}

// 改为
import whitelistData from './binance-whitelist.json';
export async function loadWhitelist(): Promise<Map<string, RiskTierKey>> {
    const parsed = whitelistData as WhitelistConfig;
    // ...
}
```

### 修复 6: TypeScript 类型错误

**问题 1:** `RiskManagerDecision` 类型未定义
**解决：** 在 `types.ts` 添加：
```typescript
export interface RiskManagerDecision {
    masterRiskScore: number;
    reason: string;
}
```

**问题 2:** `UserSessionConfig.id` 不存在
**解决：** 改为使用固定值 `'local-session'`

**问题 3:** `logPrompt` context 缺少必需属性
**解决：** 补充完整的 context 对象：
```typescript
context: {
    riskState,
    clamp,
    exposureNowUsd: 0,
    capUsd: 0,
    allowedPairs: config.allowedPairs || [],
    klineMeta: null,
}
```

## 当前状态

### ✅ 已解决
1. 所有 TypeScript 编译错误已修复
2. 所有 linter 错误已清除
3. Vite 开发服务器可以成功启动在端口 3005
4. 无 esbuild 语法错误

### ❌ 仍然存在的问题
1. **浏览器白屏**：页面无法渲染任何内容
2. **WebSocket 连接失败**：HMR (热模块替换) 无法工作
3. **"node:fs/promises" 错误仍然出现**：尽管已修复 `binance-whitelist.ts`，错误依然存在
4. **Cloudflare Workers 连接失败**：疑似网络或代理问题

### 浏览器控制台完整错误日志

```
[Audit] ERROR: [ExchangeRiskWatcher] CRITICAL: Exchange status check is a STUB
[Audit] INFO: ExchangeRiskWatcherService initialized
[Strategist] GPT_API_KEY environment variable not found
[Strategist] DEEPSEEK_API_KEY environment variable not found
[Audit] INFO: Strategist initialized
[Audit] INFO: AccountSyncService initialized

Uncaught Error: Module "node:fs/promises" has been externalized for browser compatibility. 
Cannot access "node:fs/promises.readFile" in client code.
    at Object.get (promises:3:13)
    at binance-whitelist.ts:1:26

[vite] server connection lost. Polling for restart...
WebSocket connection to 'ws://localhost:3005/' failed (多次重试)
```

## 技术栈详情

### package.json 依赖
```json
{
  "dependencies": {
    "@google/genai": "^1.27.0",
    "big.js": "^6.2.2",
    "lightweight-charts": "^5.0.9",
    "openai": "^4.73.0",
    "prom-client": "^15.1.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "recharts": "^3.3.0",
    "sql.js": "^1.11.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/ws": "^8.5.12",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
```

### 项目结构
```
BIANCE/
├── backend/          # 后端逻辑（在浏览器中运行）
│   ├── api.ts       # 主 API 入口
│   ├── strategist/  # AI 策略模块
│   └── utils/       # 工具函数
├── config/          # 配置文件
│   └── binance-whitelist.ts
├── exchange/        # 交易所接口
│   └── binance/
├── components/      # React 组件
├── api-client.ts    # 前端 API 客户端
└── App.tsx          # 应用入口
```

### 关键发现（根本原因）

#### 1. 模块导入链分析

**完整导入链：**
```
前端组件 (useDashboard.ts, AccountOverviewHeader.tsx, etc.)
    ↓ import
api-client.ts (38行从 backend/api 导入 20+ 个函数)
    ↓ import
backend/api.ts
    ↓ import (多个依赖)
├── exchange/binance/bootstrap.ts
│   └── config/binance-whitelist.ts (使用 node:fs/promises) ❌
├── exchange/binance/execution/engine.ts (使用 node:crypto) ❌
├── exchange/binance/paper/engine.ts (使用 node:crypto) ❌
├── exchange/binance/rest/client.ts (使用 node:crypto, node:timers) ❌
├── exchange/binance/ws/baseStream.ts (使用 node:events, node:timers) ❌
├── backend/orders/orderStore.ts (使用 node:fs, node:crypto) ❌
├── backend/audit/shadowReport.ts (使用 node:fs) ❌
├── config/paper-config.ts (使用 node:fs/promises) ❌
└── monitoring/prometheus.ts (使用 node:http) ❌
```

**使用 api-client.ts 的前端文件：**
1. `hooks/useDashboard.ts`
2. `hooks/useDashboardPolling.ts`
3. `components/dashboard/AccountOverviewHeader.tsx`
4. `components/trade-layout/HeaderBar.tsx`
5. `components/settings/sections/ExchangeSection.tsx`
6. `components/settings/sections/EnvironmentSection.tsx`
7. `components/settings/sections/RiskSection.tsx`

#### 2. 架构问题诊断

**问题性质：** 
这是一个**架构设计错误**，而不是简单的 bug。项目试图将需要 Node.js 运行时的后端代码（文件系统、加密、HTTP 服务器等）打包到浏览器前端。

**为什么会这样：**
- `api-client.ts` 的第 38 行直接从 `./backend/api` 导入了大量后端函数
- 这些函数依赖整个 backend 生态系统，包括币安交易所 API、文件存储、加密签名等
- Vite 在构建时尝试将所有依赖打包，遇到 Node.js 模块时失败

**为什么之前的修复无效：**
- 只修复 1-2 个文件（如 binance-whitelist.ts）无济于事
- 导入链中有 15+ 个文件使用 Node.js 模块
- 即使全部修复，也违背了 Node.js 模块的设计初衷（如 crypto.createHmac 在浏览器中不可用）

#### 3. Vite 配置分析

当前 `vite.config.ts` **没有** 配置任何 Node.js polyfills 或模块排除：
```typescript
// 缺少以下配置：
resolve: {
  alias: {
    // 没有 Node.js 模块的 polyfill 或排除
  }
},
optimizeDeps: {
  exclude: [] // 没有排除 Node.js 模块
}
```

Vite 默认会尝试打包所有导入的模块，遇到 `node:*` 前缀时会自动外部化，但这会导致运行时错误。

## 可能的解决方案（需要专家验证）

### ⭐ 方案 1: 完全重构 - 前后端分离（推荐）

**这是最彻底的解决方案，但工作量最大。**

#### 架构调整：
```
当前架构（错误）:
┌─────────────────────────────────┐
│   Browser (Vite + React)        │
│  ┌──────────────────────────┐  │
│  │  Frontend Components     │  │
│  │         ↓                │  │
│  │  api-client.ts           │  │
│  │         ↓                │  │
│  │  backend/api.ts ❌       │  │
│  │  (Node.js 代码在浏览器)   │  │
│  └──────────────────────────┘  │
└─────────────────────────────────┘

推荐架构（正确）:
┌──────────────────┐       ┌──────────────────┐
│   Browser        │       │   Node.js Server │
│  ┌────────────┐  │       │  ┌────────────┐  │
│  │ React UI   │  │◄─HTTP─┤  │ backend/   │  │
│  │            │  │       │  │ exchange/  │  │
│  │ api-client │  │◄─WS──►│  │ config/    │  │
│  └────────────┘  │       │  └────────────┘  │
└──────────────────┘       └──────────────────┘
```

#### 实施步骤：
1. **创建独立的 Node.js 后端服务**
   - 将 `backend/`, `exchange/`, `config/` 移到独立的 Node.js 项目
   - 使用 Express/Fastify 提供 REST API
   - 使用 WebSocket 推送实时数据

2. **修改 api-client.ts**
   - 移除所有 `from './backend/api'` 导入
   - 改为 HTTP fetch 调用后端 API
   - 例如：
   ```typescript
   export async function getDashboardState(): Promise<DashboardState> {
     const response = await fetch('http://localhost:4000/api/dashboard');
     return response.json();
   }
   ```

3. **更新前端组件**
   - 保持现有组件不变
   - 只需确保 api-client.ts 的接口签名一致

4. **部署**
   - 前端：静态文件托管（Vercel, Netlify, etc.）
   - 后端：Node.js 服务器（自托管或云服务）

#### 优势：
- ✅ 彻底解决浏览器兼容性问题
- ✅ 清晰的职责分离
- ✅ 独立部署和扩展
- ✅ 符合现代 Web 应用最佳实践

#### 劣势：
- ❌ 需要大量重构（估计 3-5 天）
- ❌ 需要维护两个独立的服务
- ❌ 增加部署复杂度

---

### 方案 2: 使用 Node.js Polyfills（权宜之计，不推荐）

**尝试在浏览器中模拟 Node.js 环境，但这违背了设计原则。**

#### 安装 polyfills：
```bash
npm install --save-dev vite-plugin-node-polyfills
npm install crypto-browserify stream-browserify
```

#### 修改 vite.config.ts：
```typescript
import { nodePolyfills } from 'vite-plugin-node-polyfills';

export default defineConfig({
  plugins: [
    react(),
    nodePolyfills({
      // 启用特定 polyfills
      globals: {
        Buffer: true,
        global: true,
        process: true,
      },
      protocolImports: true,
    }),
  ],
  resolve: {
    alias: {
      crypto: 'crypto-browserify',
      stream: 'stream-browserify',
      // fs 无法 polyfill，需要移除所有 fs 操作
    }
  }
})
```

#### 问题：
- ❌ `fs` (文件系统) 无法在浏览器中 polyfill
- ❌ `http.createServer` 无法在浏览器中运行
- ❌ 即使部分工作，性能和安全性都有问题
- ❌ 打包体积会急剧增大

---

### 方案 3: 条件导入 + Mock（中等工作量）

**在开发环境中 mock 后端功能，生产环境连接真实后端。**

#### 实施：
1. **创建 mock 实现**
   ```typescript
   // api-client.mock.ts
   export async function getDashboardState(): Promise<DashboardState> {
     return {
       // 返回假数据用于前端开发
     };
   }
   ```

2. **条件导入**
   ```typescript
   // api-client.ts
   const isBrowser = typeof window !== 'undefined';
   
   export const {
     getDashboardState,
     updateSessionConfig,
     // ...
   } = isBrowser 
     ? await import('./api-client.mock')
     : await import('./backend/api');
   ```

3. **逐步迁移**
   - 先让前端能启动（使用 mock）
   - 再逐步实现真实的后端 API

#### 优势：
- ✅ 前端可以独立开发
- ✅ 可以逐步迁移
- ✅ 不需要立即重构整个架构

#### 劣势：
- ❌ 需要维护两套代码（真实 + mock）
- ❌ 开发环境和生产环境行为不一致

---

### 方案 4: Electron 应用（如果允许桌面应用）

**将项目改为 Electron 桌面应用，可以同时使用 Node.js 和浏览器环境。**

#### 实施：
```bash
npm install --save-dev electron electron-builder
```

#### 优势：
- ✅ 可以直接使用所有 Node.js 模块
- ✅ 无需修改现有后端代码
- ✅ 适合量化交易系统（桌面应用更安全）

#### 劣势：
- ❌ 不再是 Web 应用
- ❌ 需要为不同操作系统打包
- ❌ 无法通过浏览器访问

## 需要社区/专家帮助的关键问题

### 核心问题

**我有一个 Vite + React 前端项目，但 `api-client.ts` 直接导入了大量需要 Node.js 运行时的后端代码（使用 fs, crypto, http, events 等模块）。导致浏览器白屏，错误为 "Module has been externalized for browser compatibility"。**

### 具体技术问题

1. **架构决策：** 
   - 是否应该将 `backend/`, `exchange/`, `config/` 完全移到独立的 Node.js 服务？
   - 还是有办法在 Vite 中配置让这些代码在"服务端"运行？

2. **Vite SSR 可行性：**
   - 可以使用 Vite 的 SSR (Server-Side Rendering) 功能让后端代码在 Node.js 环境中运行吗？
   - 如何配置 Vite 让部分代码仅在服务端执行？

3. **Polyfills 的局限性：**
   - 我知道可以用 `vite-plugin-node-polyfills`，但 `fs` 模块无法 polyfill
   - 有没有办法在构建时剔除所有 `fs` 相关代码，同时保持其他功能？

4. **动态导入 + 环境检测：**
   - 可以用 `import.meta.env.SSR` 或类似机制条件性导入 Node.js 模块吗？
   - 如何让 Vite 在浏览器端跳过这些导入？

5. **工作区/Monorepo 方案：**
   - 是否应该使用 pnpm workspaces 或 Turborepo 将前后端分离但保持在同一仓库？
   - 有没有成熟的模板可以参考？

### 约束条件

- ✅ 可以接受架构重构
- ✅ 可以改为前后端分离
- ✅ 可以使用 Electron（但希望先尝试 Web 方案）
- ❌ 不希望完全重写所有业务逻辑
- ❌ 希望尽可能复用现有代码

### 期望结果

- 浏览器能正常访问 `http://localhost:3005/`
- React 界面能正常渲染
- 前端能调用后端功能（通过某种方式）

## 相关文件列表

### 已修改的文件
1. `backend/strategist/riskManagerDecision.ts`
2. `backend/strategist/riskPromptSections.ts` (新建)
3. `backend/utils/EventEmitter.ts` (新建)
4. `backend/exchangeRiskWatcher.ts`
5. `config/binance-whitelist.ts`
6. `backend/api.ts`
7. `types.ts`
8. `backend/strategist/modelDecision.ts`

### 疑似问题文件（全局搜索发现）

**使用 Node.js `fs` 模块的文件：**
1. `backend/audit/shadowReport.ts` - 使用 `node:fs`, `node:fs/promises`
2. `backend/orders/orderStore.ts` - 使用 `node:fs`, `node:fs/promises`
3. `config/paper-config.ts` - 使用 `node:fs/promises`
4. `config/binance-risk-config.ts` - 使用 `node:fs/promises`
5. `exchange/binance/execution/shadowAuditor.ts` - 使用 `node:fs/promises`

**使用 Node.js `crypto` 模块的文件：**
6. `exchange/binance/execution/engine.ts` - 使用 `node:crypto`
7. `exchange/binance/paper/engine.ts` - 使用 `node:crypto`
8. `exchange/binance/rest/client.ts` - 使用 `node:crypto`
9. `backend/orders/orderStore.ts` - 使用 `node:crypto`

**使用 Node.js `events` 模块的文件：**
10. `exchange/binance/ws/baseStream.ts` - 使用 `node:events`

**使用其他 Node.js 模块的文件：**
11. `monitoring/prometheus.ts` - 使用 `node:http`
12. `exchange/binance/ws/baseStream.ts` - 使用 `node:timers/promises`
13. `exchange/binance/ws/userStream.ts` - 使用 `node:timers`
14. `exchange/binance/rest/client.ts` - 使用 `node:timers/promises`, `node:url`
15. `exchange/binance/cache/exchangeInfoCache.ts` - 使用 `node:timers/promises`
16. `exchange/binance/time/sync.ts` - 使用 `node:timers/promises`

**⚠️ 重要发现：** 
整个项目有 **15+ 个文件** 在使用 Node.js 专用模块，这些模块在浏览器环境中无法运行。这表明项目架构存在根本性问题：**后端代码和前端代码严重耦合，没有清晰的边界。**

---

## 附录：有用的命令和参考

### 诊断命令

```bash
# 查找所有使用 Node.js 模块的文件
grep -r "from 'node:" --include="*.ts" --include="*.tsx"

# 查看导入链
npm ls <package-name>

# 清理缓存和重新安装
rm -rf node_modules package-lock.json
npm install

# 查看 Vite 构建分析
npm run build -- --mode development
```

### 相关资源

1. **Vite 官方文档 - 浏览器兼容性**
   https://vitejs.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility

2. **Node.js Polyfills for Vite**
   https://github.com/davidmyersdev/vite-plugin-node-polyfills

3. **前后端分离最佳实践**
   - REST API: https://restfulapi.net/
   - WebSocket: https://socket.io/

4. **Monorepo 工具**
   - Turborepo: https://turbo.build/
   - pnpm workspaces: https://pnpm.io/workspaces

5. **Electron 入门**
   https://www.electronjs.org/docs/latest/tutorial/quick-start

### 社区求助平台

- Stack Overflow: https://stackoverflow.com/questions/tagged/vite
- Vite Discord: https://chat.vitejs.dev/
- Reddit r/webdev: https://reddit.com/r/webdev
- GitHub Discussions: https://github.com/vitejs/vite/discussions

---

## 文档元数据

**生成时间**: 2025-01-15  
**问题追踪**: 已进行超过 20 次修复尝试，问题仍未完全解决  
**文档版本**: 1.0  
**作者**: BIANCE 开发团队  
**状态**: 待社区/专家帮助  

**下一步行动**:
1. ✅ 将此文档分享到技术社区
2. ⏳ 评估各个解决方案的可行性
3. ⏳ 决定是否进行架构重构
4. ⏳ 如选择重构，制定详细的迁移计划

